server:
  port: 8084

spring:
  application:
    name: creditcard-microservice
  datasource:
    url: jdbc:mysql://localhost:3306/creditcarddb
    username: root
    password: Manish@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
